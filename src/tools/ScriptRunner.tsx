import { styled } from "solid-styled-components";

const Container = styled("div")`
  flex: 1;
  flex-direction: column;
`;

export const ScriptRunner = () => {
  return <Container>Script runner</Container>;
};
